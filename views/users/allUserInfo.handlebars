{{#if users.length}}
    <ul>
      {{#each users}}
        <li>
          <a href="/users/{{this._id}}"> {{this.firstName}} from {{this.location}}, ID: {{this._id}}</a>
        </li>
      {{/each}}
    </ul>
{{/if}}

